{
  "scripts": {
    "build": "bun script/build.js",
    "buildZip": "bun script/build-zip.js",
    "buildExe": "bun script/build-zip.js && bun build --compile --minify  script/build-exe.js --outfile main",
    "server": "bun index.html",
    "test": "bun test/test.js"
  },
  "dependencies": {
    "katex": "^0.16.21",
    "marked": "^15.0.7",
    "ollama": "^0.5.14"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16"
  }
}
